# Optional proxy mapping for run_remote.sh
#
# Copy this file to `proxy-conf.yml` if you need proxies.
# The top-level keys are remote host/IP names (matching remote-conf.host).

101.42.249.205:
  type: socks5
  host: proxy.xxx.com
  port: 1080
